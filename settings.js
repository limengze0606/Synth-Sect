// 定義稀有度和對應的圖片尺寸
const Rarity = {
    Normal: {
        pictureWidth: 300,
        pictureHeight: 200,
    },
    FullArt: {
        pictureWidth: cardWidth, // 使用全卡面寬度
        pictureHeight: cardHeight, // 使用全卡面高度
    }
};